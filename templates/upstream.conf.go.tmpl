# Autogenerated upstream file for {{ .Upstream }}

upstream {{ .Upstream }} {
    {{- if .Options }}
      {{- range $key, $value := .Options }}
    {{ $key }}{{ if $value }} {{ $value }}{{ end }};
      {{- end }}
    {{ end }}
    {{- range $server := .Servers }}
    server {{ $server }};
    {{- end }}
}
